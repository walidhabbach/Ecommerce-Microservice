apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-customer-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "100Mi"
  volumeName: mongo-customer-pv